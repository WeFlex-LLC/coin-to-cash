{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "package.json" }
  ],
  "build": {
    "functions": {
      "src": "src/**/*.ts",
      "output": "dist"
    }
  }
}
